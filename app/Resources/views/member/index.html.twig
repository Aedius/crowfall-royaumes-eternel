{% extends 'global/base.html.twig' %}

{% block body %}

                        <a href="{{ path('writer-article-edit') }}">nouvel article</a>
                        <a href="#!" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
                            {{ app.user.username }} <span class="label">Sans guilde</span>
                        </a>
                        <div class="dropdown-menu">
                            <ul role="menu">
                                {% if is_granted("ROLE_WRITER") %}
                                    <li>
                                        <a href="{{ path('writer-article-edit') }}">nouvel article</a>
                                    </li>
                                {% endif %}


                                {% if is_granted("ROLE_ADMIN") %}
                                    <li class="dropdown dropdown-submenu pull-left">
                                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"
                                           role="button"
                                           aria-expanded="false">Admin</a>
                                        <div class="dropdown-menu">
                                            <ul role="menu">
                                                <li>
                                                    <a href="{{ path('admin-category-edit') }}">New Category</a>
                                                </li>
                                                <li>
                                                    <a href="#">Version</a>
                                                </li>
                                            </ul>
                                        </div>
                                    </li>
                                {% endif %}

                            </ul>
                        </div>

{% endblock %}